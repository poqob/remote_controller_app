what if creating a new model for dynamic behaviour pad??

suppose:
    actions: move(the critical point) -+(x,y), click(done), scroll(done)

    move mechanics:
        ------------------------------




        web projesi uçak rezervasyon ya da hastane rezervasyon.

